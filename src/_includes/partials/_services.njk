<!-- services -->
<section id="services" class="pb-20">
<div class="max-w-screen-xl m-auto px-5">
  <div class="grid gap-10 grid-cols-1 sm:grid-cols-2 md:grid-cols-3 border-t-4 border-gray-200 dark:border-gray-800 py-10">
    {% for item in data.services %}
      {% if item.name === 'Services' %}
        <div class="sm:col-span-2 md:col-span-3 md:mb-5 sm:w-4/6">
          <h3 class="text-rose-700 mb-10 text-xs font-medium uppercase dark:text-gray-400">{{ item.name }}</h3>
          <p class="font-medium">{{ item.description }}</p>
        </div>
        {% else %}
        <div>
          <h3 class="font-semibold dark:text-white">{{ item.name }}</h3>
          <p class="text-gray-600 md:pb-5 post dark:text-gray-200">{{ item.description | safe }}</p>
          <p class="leading-8">
            {% for item in item.tags%}
            <span class="bg-gray-100 dark:bg-gray-800 text-gray-800 dark:text-gray-100 text-xs py-1 px-2 whitespace-nowrap rounded">{{item}}</span>
            {% endfor %}
          </p>
        </div>
        {% endif %}
      {% endfor %}
    </div>
  </div>
</section>
<!-- services end -->