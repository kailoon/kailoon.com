<div class="relative max-w-screen-md mx-auto py-32 md:py-48 md:leading-tight text-center font-bold tracking-tighter text-3xl md:text-5xl px-10">
  <span class="block">🤝</span>
  I am taking new projects now. <br>If you happen to have one, please get in touch.
  <div>
    <a
      href="{{ metadata.author.mailtolink }}"
      title="get in touch"
      target="_blank"
      rel="noreferrer"
      class="text-gray-600 text-xl tracking-tight py-4 dark:text-yellow-200 dark:hover:text-yellow-400 transition-colors hover:text-blue-800"
    >via Email
    </a>
  </div>
</div>

<!-- footer start -->
<footer id="footer" class="md:pt-6 md:pb-16 border-t border-gray-200 dark:border-gray-800">
  <div class="text-center md:text-left max-w-screen-xl m-auto sm:flex sm:justify-between sm:items-center text-sm pb-16 md:pb-0">
    <p id="credit" class="p-5 text-gray-500 dark:text-gray-400">{{ data.footerCreditLine1 }} {{ data.footerCreditLine2 }}
      {% for item, url in data.builtTools %}
        {%- if loop.first %} {% elif loop.last %} &{% else %},{% endif %}
        <a href="{{ url }}" class="text-black hover:text-blue-800 dark:text-gray-200 dark:hover:text-yellow-200" target="_blank" rel="noreferrer" title="follow me on social media" class="shadow md:transition-colors sm:hover:text-blue-800 md:hover:shadow-md">{{ item }}</a>
        {%- if loop.last -%}. {%- endif -%}
      {% endfor %}
    </p>

    <p class="p-5">
      <span class="inline-flex space-x-5 leading-tight">
        {% for item, url in metadata.socials %}
          <a href="{{ url }}" target="_blank" rel="noreferrer" class="font-bold text-black hover:text-blue-800 dark:text-gray-200 dark:hover:text-yellow-200" target="_blank" rel="noreferrer" title="follow me on social">{{ item }}.</a>
        {% endfor %}
      </span>
    </p>

    <!-- sticky footer for mobile -->
    <div id="sticky-footer"
      class="md:hidden text-base w-full px-5 flex justify-between items-center fixed bottom-0 bg-black md:relative dark:bg-blue-900">
      {% set navPages = collections.all | eleventyNavigation %}
        {%- for entry in navPages %}
          <a
          href="{{ entry.url | url }}"
          class="text-gray-300 py-4 dark:text-blue-300
          {% if entry.url == page.url %} text-white dark:text-white {% endif %}
          "
          >{{ entry.title }}</a>
      {%- endfor %}
        <a
          href="{{ metadata.author.mailtolink }}"
          title="get in touch"
          target="_blank"
          rel="noreferrer"
          class="text-gray-300 py-4 dark:text-blue-300"
        >Contact
        </a>
    </div>
  </div>
</footer>
<!-- footer end -->